apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ocirepository.yaml
  - helmrelease.yaml
configMapGenerator:
  - name: kgateway-values
    namespace: kgateway-system
    files:
      - values.yaml=kgateway-values.yaml
configurations:
  - kustomizeconfig.yaml