---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./a2a.yaml
  - ./github-webhook.yaml
  # Disabled: references gateway.kgateway.dev/Backend kind not supported in current CRD version
  # - ./youtube-mcp.yaml
  # Claude/Gemini multi-model routes, all on the single shared agentgateway
  # Gateway (path-prefix per model, OpenAI-compatible endpoints)
  - ./claude-routes.yaml
  - ./gemini-routes.yaml
