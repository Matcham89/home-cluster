# PodMonitor for base agentgateway
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: agentgateway
  namespace: monitoring
  labels:
    app: agentgateway
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      gateway.networking.k8s.io/gateway-name: agentgateway
  namespaceSelector:
    matchNames:
      - kgateway-system
  podMetricsEndpoints:
  - port: metrics
    path: /metrics
    interval: 30s
    scrapeTimeout: 10s
    relabelings:
      - sourceLabels: [__meta_kubernetes_pod_name]
        targetLabel: pod
      - sourceLabels: [__meta_kubernetes_namespace]
        targetLabel: namespace
      - targetLabel: gateway_instance
        replacement: gateway0
    metricRelabelings:
      - sourceLabels: [__name__]
        regex: '.*_info'
        action: drop
---
# PodMonitor for agentgateway1
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: agentgateway1
  namespace: monitoring
  labels:
    app: agentgateway1
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      gateway.networking.k8s.io/gateway-name: agentgateway1
  namespaceSelector:
    matchNames:
      - kgateway-system
  podMetricsEndpoints:
  - port: metrics
    path: /metrics
    interval: 30s
    scrapeTimeout: 10s
    relabelings:
      - sourceLabels: [__meta_kubernetes_pod_name]
        targetLabel: pod
      - sourceLabels: [__meta_kubernetes_namespace]
        targetLabel: namespace
      - targetLabel: gateway_instance
        replacement: gateway1
    metricRelabelings:
      - sourceLabels: [__name__]
        regex: '.*_info'
        action: drop
---
# PodMonitor for agentgateway2
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: agentgateway2
  namespace: monitoring
  labels:
    app: agentgateway2
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      gateway.networking.k8s.io/gateway-name: agentgateway2
  namespaceSelector:
    matchNames:
      - kgateway-system
  podMetricsEndpoints:
  - port: metrics
    path: /metrics
    interval: 30s
    scrapeTimeout: 10s
    relabelings:
      - sourceLabels: [__meta_kubernetes_pod_name]
        targetLabel: pod
      - sourceLabels: [__meta_kubernetes_namespace]
        targetLabel: namespace
      - targetLabel: gateway_instance
        replacement: gateway2
---
# PodMonitor for agentgateway3
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: agentgateway3
  namespace: monitoring
  labels:
    app: agentgateway3
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      gateway.networking.k8s.io/gateway-name: agentgateway3
  namespaceSelector:
    matchNames:
      - kgateway-system
  podMetricsEndpoints:
  - port: metrics
    path: /metrics
    interval: 30s
    scrapeTimeout: 10s
    relabelings:
      - sourceLabels: [__meta_kubernetes_pod_name]
        targetLabel: pod
      - sourceLabels: [__meta_kubernetes_namespace]
        targetLabel: namespace
      - targetLabel: gateway_instance
        replacement: gateway3

---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: claude-sonnet
  namespace: monitoring
  labels:
    app: claude-sonnet
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      gateway.networking.k8s.io/gateway-name: agentgateway1
  namespaceSelector:
    matchNames:
      - kgateway-system
  podMetricsEndpoints:
  - port: metrics
    path: /metrics
    interval: 30s
    scrapeTimeout: 10s
    relabelings:
      - sourceLabels: [__meta_kubernetes_pod_name]
        targetLabel: pod
      - sourceLabels: [__meta_kubernetes_namespace]
        targetLabel: namespace
      - targetLabel: gateway_instance
        replacement: claude-sonnet
    metricRelabelings:
      - sourceLabels: [__name__]
        regex: '.*_info'
        action: drop
---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: claude-opus
  namespace: monitoring
  labels:
    app: claude-opus
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      gateway.networking.k8s.io/gateway-name: agentgateway2
  namespaceSelector:
    matchNames:
      - kgateway-system
  podMetricsEndpoints:
  - port: metrics
    path: /metrics
    interval: 30s
    scrapeTimeout: 10s
    relabelings:
      - sourceLabels: [__meta_kubernetes_pod_name]
        targetLabel: pod
      - sourceLabels: [__meta_kubernetes_namespace]
        targetLabel: namespace
      - targetLabel: gateway_instance
        replacement: claude-opus
---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: claude-haiku
  namespace: monitoring
  labels:
    app: claude-haiku
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      gateway.networking.k8s.io/gateway-name: agentgateway3
  namespaceSelector:
    matchNames:
      - kgateway-system
  podMetricsEndpoints:
  - port: metrics
    path: /metrics
    interval: 30s
    scrapeTimeout: 10s
    relabelings:
      - sourceLabels: [__meta_kubernetes_pod_name]
        targetLabel: pod
      - sourceLabels: [__meta_kubernetes_namespace]
        targetLabel: namespace
      - targetLabel: gateway_instance
        replacement: claude-haiku
