apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: zitadel-operator
  namespace: flux-system
spec:
  chart:
    spec:
      chart: zitadel
      version: 8.13.4
      sourceRef:
        kind: HelmRepository
        name: zitadel
        namespace: flux-system
  interval: 1m0s
  values:
    global:
      image:
        tag: v1.8.11
