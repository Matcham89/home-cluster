---
apiVersion: v1
kind: Service
metadata:
  name: whatsapp-mcp
  namespace: kagent
  labels:
    app: whatsapp-mcp
    app.kubernetes.io/name: whatsapp-mcp
    istio.io/use-waypoint: "none"
spec:
  selector:
    app: whatsapp-mcp
  ports:
    - port: 3000
      targetPort: 3000
      name: http
  type: ClusterIP
