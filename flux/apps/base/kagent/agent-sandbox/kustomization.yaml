---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Upstream release manifest (kubernetes-sigs/agent-sandbox v0.4.6).
  # Vendored because the project ships kubectl-apply manifests, not a Helm chart.
  # Update manually: re-download manifest.yaml from a newer release tag at
  # https://github.com/kubernetes-sigs/agent-sandbox/releases
  - ./manifest.yaml
