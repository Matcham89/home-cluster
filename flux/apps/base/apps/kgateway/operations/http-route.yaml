apiVersion: gateway.networking.k8s.io/v1beta1
kind: HTTPRoute
metadata:
  name: agentgateway-admin
spec:
  parentRefs:
    - name: agentgateway
  hostnames:
    - "agentgateway.kubegit.com"
  rules:
    - matches:
        - path:
            type: Prefix
            value: /
      backendRefs:
        - name: agentgateway-admin
          port: 80
