---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kiali
spec:
  interval: 1m
  chart:
    spec:
      chart: kiali-operator
      version: 2.18.0
      sourceRef:
        kind: HelmRepository
        name: kiali
  install:
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3

