# ServiceMonitors removed - non-essential monitoring
# Cert-Manager health is monitored via:
# - Pod status (kubectl get pods -n kube-ops)
# - Certificate Ready status (kubectl get certificates -A)
# - Certificate expiration (visible in certificate resources)
#
# Removed monitors:
# - cert-manager controller
# - cert-manager cainjector
# - cert-manager webhook
