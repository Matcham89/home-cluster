# ServiceMonitor removed - non-essential monitoring
# Istio health is monitored via:
# - Pod status (kubectl get pods -n istio-system)
# - Gateway status (kubectl get gateway -A)
# - HTTPRoute status (kubectl get httproute -A)
#
# Removed monitors:
# - istiod control plane
