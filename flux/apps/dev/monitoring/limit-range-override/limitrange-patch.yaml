---
# LimitRange patch for monitoring namespace
# Monitoring tools (Prometheus, Grafana) need more resources
# Higher defaults to prevent OOMKills during startup
apiVersion: v1
kind: LimitRange
metadata:
  name: default-limits
spec:
  limits:
  - default:
      cpu: 500m
      memory: 1Gi
    defaultRequest:
      cpu: 100m
      memory: 128Mi
    type: Container
